import os
import sys
import configure_and_run

if __name__ == "__main__":
    # Add script path into PYTHONPATH
    # script_path = os.path.dirname(os.path.realpath(__file__))
    # sys.path.append(script_path)
    pass
